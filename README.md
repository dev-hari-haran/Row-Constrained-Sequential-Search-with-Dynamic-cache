# Row-Constrained-Sequential-Search-with-Dynamic-cache
Database-Guided Grid Exploration Bot is a PyQt6 visualization of a knowledge-driven search algorithm. The bot uses a reference database to restrict its search to expected rows, explores a disarranged grid under strict movement constraints, and expands to adjacent rows only when the expected location fails.
